@model IEnumerable<Calories.Models.Food>
@{
    ViewBag.Title = "Foods";
    Layout = "/Views/Shared/_Layout.cshtml";

}


<div>
    @foreach (Food f in Model)
    {
        <button value="@f.ID">@f.Name</button>
    }
</div>
